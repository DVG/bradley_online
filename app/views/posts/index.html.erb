<%= div_for(@posts, :class => "post") do |post| %>
  <div class="post-title page-header">
    <h2><%= post.title %></h2>
  </div>
  <div class="post-body">
    <%= markdown post.body %>
  </div>
<% end %>